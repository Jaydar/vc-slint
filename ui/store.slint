export global AppData {
    in-out property <bool> is-show-side-bar: false;
    in-out property <string> router: "home";
    pure callback ColorToString(color) -> string;
    pure callback StringToColor(string) -> color;
}
